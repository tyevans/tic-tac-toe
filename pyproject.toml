[project]
name = "tic-tac-toe"
version = "0.1.0"
description = "A strongly domain-driven tic-tac-toe"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tictactoe"]

